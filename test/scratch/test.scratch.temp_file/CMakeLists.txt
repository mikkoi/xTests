if(WIN32)
    add_executable(test.scratch.temp_file test.scratch.temp_file.cpp)
    #add_dependencies (test.scratch.temp_file )
    target_link_libraries(test.scratch.temp_file xTests.core STLSoft::STLSoft)
    add_test(NAME test.scratch.temp_file COMMAND ./test.scratch.temp_file)
endif()
